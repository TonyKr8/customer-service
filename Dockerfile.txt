FROM nginx:alpine

# העתקת קוד האתר לתיקיית ברירת המחדל של nginx
COPY index.html /usr/share/nginx/html/

# התאמת הרשאות עבור OpenShift (שאינו רץ כ-root כברירת מחדל)
RUN chmod -R 777 /var/cache/nginx /var/run /var/log/nginx

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]